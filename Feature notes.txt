Develop a plan for the following:

Take a look in the comfyapi directory and develop a new feature for Synesthesia to talk to a comfyui api in addition to LTX -- similar to how reimagine.py does it.  We will make use of the existing ZImage_Poster_API.json file also.  For now this feature will only be for Z-image first frame and only needs to support this one json file.  

Two GUI updates will be needed.  When Z-image First Frame is selected, spawn a drop-down to select between the LTX-desktop or Comfy API's.  On tab 6 add a spot for the user to add the Comfy API location.        

Also think about any gotcha's or inconsistencies with this plan and develop fixes before we start.

